import styled from "styled-components";

export const Users = () => {
  return (
    <SContainer>
      <h2>USERS Page here</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
